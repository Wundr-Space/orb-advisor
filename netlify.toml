[build]
  command = "npm run build"
  publish = "dist"

# Keep static assets untouched, then route app paths to index.html
[[redirects]]
  from = "/assets/*"
  to = "/assets/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
